# hospcall-backoffice

Operator console for **automated vet-clinic outreach (HOSPCALL)**.

```bash
make install
make dev          # http://localhost:3000 → /hospitals
```

Backend: `hospcall-server` running on `:8002`. See [`CLAUDE.md`](./CLAUDE.md).
